pandas>=2.0
numpy>=1.24
matplotlib>=3.7
PyYAML>=6.0
tabulate>=0.9
python-dateutil>=2.8

# Optional: only needed to pull real historical data from Binance.
# The framework works fully offline with CSV or synthetic data without it.
ccxt>=4.0

# Dev/test
pytest>=7.4
pytest-cov>=4.1
